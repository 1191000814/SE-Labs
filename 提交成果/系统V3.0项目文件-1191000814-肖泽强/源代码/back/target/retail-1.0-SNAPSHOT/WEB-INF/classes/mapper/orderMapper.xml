<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.OrderMapper">
  <select id="getById" resultType="pojo.OrderVo">
    select * from retail.order where id = #{id}
  </select>

  <select id="getAll" resultType="pojo.OrderVo">
    select * from retail.order
  </select>

  <select id="getMaxId" resultType="int">
    select max(id) from retail.order
  </select>

  <select id="getByCustomer" resultType="pojo.OrderVo">
    select * from retail.order where customer_id=#{customerId}
  </select>

  <delete id="remove">
    delete from retail.order where id = #{id}
  </delete>

  <update id="modify">
    update retail.order
    set items=#{items}, create_date=#{createDate}, im_date=#{imDate}, price=#{price}, customer_id=#{customerId}, profit=#{profit}
    where id = #{id}
  </update>

  <insert id="add" useGeneratedKeys="true" keyProperty="id">
    insert into retail.order values (#{id},#{items},#{createDate},#{imDate},#{price},#{customerId},#{profit})
  </insert>

</mapper>