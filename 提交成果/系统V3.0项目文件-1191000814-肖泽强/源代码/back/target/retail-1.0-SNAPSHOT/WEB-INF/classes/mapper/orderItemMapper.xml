<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.OrderItemMapper">
  <select id="getById" resultType="pojo.OrderItemVo">
    select * from retail.order_item where id = #{id}
  </select>

  <select id="getAll" resultType="pojo.OrderItemVo">
    select * from retail.order_item
  </select>

  <select id="getMaxId" resultType="int">
    select ifnull(max(id), 0) from retail.order_item
  </select>

  <delete id="remove">
    delete from retail.order_item where id = #{id}
  </delete>

  <update id="modify">
    update retail.order_item
    set goods_id=#{goodsId}, num=#{num}, discount_rate=#{discountRate}
    where id = #{id}
  </update>

  <insert id="add" useGeneratedKeys="true" keyProperty="id">
    insert into retail.order_item values (#{id}, #{goodsId}, #{num}, #{discountRate})
  </insert>

</mapper>